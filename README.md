# Project Documentation

## Linting
- This project uses SwiftLint for style and lint checks.
- Install manually via Homebrew: `brew install swiftlint`.
- CI is configured to run SwiftLint on every push and pull request.